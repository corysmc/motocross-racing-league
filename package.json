{
  "name": "motocross-racing-league",
  "private": true,
  "version": "0.0.1",
  "description": "motocross-racing-league",
  "license": "MIT",
  "files": [
    "dist/"
  ],
  "scripts": {
    "build": "stencil build",
    "build.netlify": "npm test && npm run build --prod",
    "start": "stencil build --dev --watch --serve",
    "test": "stencil test --spec --e2e",
    "test.watch": "stencil test --spec --e2e --watch"
  },
  "dependencies": {
    "@ionic/core": "^4.0.1",
    "date-fns": "^2.0.0-alpha.27"
  },
  "devDependencies": {
    "@stencil/core": "^0.18.0",
    "@types/jest": "23.3.10",
    "@types/puppeteer": "1.11.1",
    "jest": "23.6.0",
    "jest-cli": "23.6.0",
    "puppeteer": "1.11.0",
    "workbox-build": "3.4.1"
  }
}
